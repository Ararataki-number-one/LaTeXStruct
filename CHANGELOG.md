# Changelog

本项目采用“手动构建不可变候选 → 四项真实验收 → tag 发布同一候选字节”的流程。
tag 触发后会重跑测试、核验验收声明并复算候选资产；不会重新构建已经验收的资产。

## Unreleased

## v2.0.0-rc.1（2026-08-24，UNVERIFIED 预发行版）

- 这是供提前试用与反馈的未验证预发行版，不是稳定版；GitHub 标签为 `v2.0.0-rc.1`，当前
  Windows 二进制沿用内部版本 `2.0.0`，稳定版自动更新通道不会主动分发本预发行版；
- 37 页真实云端诊断已完成 37/37 页 OCR 与两遍 LaTeX 编译，但诊断基线曾由 37 页膨胀为
  49 页，并发现图片尺寸失控和印刷页码混入正文。RC 代码已加入基于 bbox 的图片宽高限制、
  边界页码宿主清理以及原始/清理后文本分离，尚未完成新的不可变整本复测；
- `ocr-17`、`ocr-600`、`analysis-17`、`analysis-600` 四项同候选真实验收尚未齐全，因此本版本
  明确标记为 `UNVERIFIED`，不得作为出版质量、95% 准确率或稳定版发布通过的证明。

## v2.0.0（2026-08-24）

- 将 OCR 与后续 AI 整理拆成两个独立阶段：新增宿主权威的不可变 OCR 运行快照、稳定 `page_id`
  和逐页状态记录；页面结果与原始响应按 SHA-256 绑定并原子保存，可从已提交状态恢复且跳过已经
  校验的成功页。运行时采用有界队列、最多 3 页批量或并发；批量漏答、重复或未知 `page_id`
  会严格失败并保守降级，鉴权、额度、配置与明确拒绝不会扩增为逐页重试；
- 新增 OCR 基线门禁：失败页不能被静默遗漏，原始 OCR TEX 冻结后才进入最小语法恢复；编译证据
  严格区分 `COMPILED`、`PARTIAL_COMPILED` 与 `SOURCE_PREVIEW`。进度和性能字段读取实际逐页
  状态，记录成功、重试、待确认、失败、并发、DPI、平均与近期速度及动态 ETA；
- 新增分析整理 v2 的宿主侧质量闭环契约：不可变 `AnalysisRunSnapshot`、稳定 `PageUnit`、唯一
  `IssueLedger` 和受限状态机由程序维护；跨角色问题在宿主侧去重，已关闭问题重新出现时沿用原
  `issue_id` 并标记 `REGRESSION`，模型不能决定状态、哈希、文件角色或验证结论；
- AI 修复改用绑定当前候选 hash、原片段 hash、唯一锚点与问题范围的局部补丁；全文替换、越界、
  旧候选返回及未授权正文或数学 token 变化均拒绝。每个宏轮次候选以独立目录原子落盘并附
  `SHA256SUMS`；候选按质量向量字典序比较，高优先级指标变差时自动保留历史最佳版本，崩溃恢复
  只能从哈希完整且未被拒绝的已提交候选继续；
- 加入按源页、基线片段、当前片段、渲染、提示词、模型和审计角色组成的缓存键及局部失效；连续
  两轮无进展会停止原策略并依次升级证据范围、独立上下文与强模型裁决。`VERIFIED` 只能由全页
  覆盖、同一候选 hash、内容与数学守恒、真实编译证据及两轮不同上下文的独立复核共同产生；
- OCR 页面改为面向普通用户的文件、页范围和“快速 / 推荐 / 高质量”入口，移除 OCR 阶段的模板
  与结构整理选择；工作台增加当前轮次、已检查页面、问题账本、回滚、历史最佳、真实耗时与 ETA，
  并展示 OCR 原稿、基线 TEX/PDF 和 AI 最佳 TEX/PDF 的可用状态；
- Windows 发布资产先由手动候选 workflow 构建，再由 OCR 17/600 页与分析 17/600 页四个真实
  profile 验收；tag workflow 只下载、复算并发布该不可变候选的原始字节，不再重新构建。便携 ZIP
  必须且只能包含根目录下的 `LaTeXStruct.exe`、`LICENSE` 和 `THIRD_PARTY_NOTICES.txt`；候选同时
  生成可重算的 `release-assets.json` 与 `SHA256SUMS.txt`，安装器和便携版仍须通过版本、许可证、
  启动、健康检查与资产一致性门禁；
- 已通过真实 LaTeXStruct 页面执行一次“合成双图片项目”Codex CLI OCR：2/2 页为 `SUCCESS`、
  并发数为 2，每页模型调用约 21.4 秒且相互重叠，总墙钟约 33.6 秒，重试数为 0；原始 OCR TEX
  被冻结，基线经两遍 XeLaTeX 编译，两次均 exit 0，并生成 2 页 `COMPILED` 预览。这只是非用户
  样本的通路冒烟，不是识别质量或整书性能验收，不可外推整书吞吐；
- Codex CLI 后端现在探测并缓存实际 runtime feature registry，只发送当前版本支持的禁用项；
  启动、登录和配置失败被归类为永久错误并立即终止，不再产生无效重试。OCR 页面预览在模型调用前
  原子保存并确保持久可读，避免状态接口先报告可用、文件尚未落盘的竞态；任务总耗时覆盖合并、
  冻结、基线恢复和两遍编译，不再只统计逐页模型调用；
- 多图片输入的 provenance 由宿主严格核对原始顺序、文件名、字节数、逐图 SHA-256、固定派生标识
  与源集合哈希，缺项或不一致会闭锁。界面 100% 只表示执行计划已到终点，不代表 `VERIFIED`；
  独立全文复核、第二遍 AI 复查、编译、视觉闭环或最终门禁缺失/失败时，仍保持
  `COMPLETED_WITH_ISSUES`、`UNVERIFIED` 或失败状态；
- OCR 默认“推荐/高质量”档采用初读加五级恢复：同 DPI 重试、批量降单页、300 DPI、页图加公式
  裁片、全新上下文独立复读；每次调用的页图/裁片/响应/错误/用量/耗时均写入追加式哈希链。单页
  完成后立即落盘，不再等待同组三页；独立复读不一致或仍有未决区域只会进入 `NEEDS_REVIEW`。
  `SOURCE_PREVIEW` 现在始终是可打开且第一页明确声明“不是 LaTeX 编译结果”的 PDF；
- 发布 tag 新增真实验收声明门：17 页与 600 页必须绑定同一被测 commit、build ID、EXE SHA、模型
  调用和双遍编译证据；测试替身、缺页、超时、部分编译或验收后运行时代码变化均阻止发布；
- 当前代码级、宿主运行时测试和合成样本冒烟不等于真实模型端到端验收。v2.0.0 只有在
  `ocr-17`、`ocr-600`、`analysis-17`、`analysis-600` 四项真实验收全部通过并绑定同一候选字节后
  才能发布；单次较小样本诊断不能替代 600 页门禁，也不构成整书性能声明。
  现有“导出 TEX”“导出工程 ZIP”和“AI 审计提交包”的既有行为未作修改。

## v1.2.10（2026-08-23）

- 修复 OCR 原始 TeX 中展示数学环境空段落导致的真实编译失败：宿主只删除已解析数学环境内部的
  纯空白段落，不改写任何公式、命令或正文；本次 17 页审计样本由 `Missing $ inserted`、仅 2 页
  部分产物恢复为原始 OCR 工程完整编译成功；
- 加速结构判断：proof、scope-fix 和需要边界判定的定理仍与低风险候选分池、逐项绑定独立 ID 和
  源窗口，但每次最多合并 3 项传输；漏答、重复 ID、越界或非法范围仍整批 fail-closed。按当前
  候选集回放该样本，结构传输由逐项 11 次降为 5 次，请求正文字符量由 97,919 降至 72,773；
- 修复三个正确 theorem 结果被宿主边界门误拒绝的问题：只有定理类条目以完整展示公式闭合，且
  下一原子具有“开始证明、回顾已完成结果或引出下一正式结果”的高精度退出证据时才允许在普通
  过渡段之前结束；未闭合公式、普通限定语、definition 和弱证据仍拒绝；
- 修复模板重排在源 PDF 与成品 PDF 恰好同页数时退化为同页双射的问题：宿主现在用罕见文本锚点
  冻结可覆盖两侧全部页面的单调 warping 路径，允许自动目录、合页和拆页；reflow 页对明确只是
  内容锚点，全文字符量与文本相似度作为独立证据，不能再仅凭“左右同页文字不同”误报内容丢失；
  一个源页映射多张成品页时各页共享该源页 formal 清单，相同修复按 ID 去重、冲突修复 fail-closed；
- 逐页视觉复核支持一次携带最多 3 张独立全分辨率页对，模型仍须逐页回显冻结页码、告警和结果；
  漏答、重复或未知映射均失败，宿主继续记录每张组合页的 SHA-256；若兼容端不支持多图、请求
  超时或输出明确因 token 上限截断，则自动把该批回退为原有逐页全分辨率调用。该 17 页样本的
  视觉传输由 17 次降为 7 次；按当前候选集回放，结构与视觉
  合计为 12 次；多轮定点修复及回退中途失败的调用与 Token 现在也会完整累计。实际 token、OCR
  转写耗时和总耗时仍取决于模型、端点及页面内容；
- 失败报告按唯一候选计数，保留同一候选的全部诊断但不把 3 项重复显示成 6 项；三个核心门禁改为
  显示候选 ID、formal 行号和视觉源页，消除“结论：结论未通过”重复文案；用户关闭第二遍复查时
  明确显示“用户未启用（不计为失败）”。现有 TEX、工程 ZIP 和 AI 审计提交包导出行为不变。

## v1.2.9（2026-08-23）

- 修复 v1.2.8 OCR 导入快照漏存原 PDF 总页数的问题：宿主现在从冻结、哈希绑定的 PDF 字节重新
  解析总页数，并与任务启动快照逐项核对；缺失、布尔伪装、越界或不一致的页数/范围均在项目发布前
  fail-closed，OCR manifest 同时显式记录源文件总页数；
- 为受影响的旧项目加入证据驱动迁移：只有最新已提交的不可变审计快照通过 descriptor、控制文件、
  blob、工件身份和指纹校验，并独立证明同一 PDF 的真实页数与连续选择范围时，才把错误的
  `source_pages=1` 修复为实际页数；迁移记录绑定源 SHA-256 与权威 snapshot ID，其他异常仍拒绝
  自动修复。真实 17 页项目已做只读回放验证，无需重新 OCR；
- 任务状态新增“输入证据检查”阶段，并把终态审计材料保存从业务进度中分离；预检失败会显示真实的
  失败阶段和 1% 位置，不再因错误日志保存而误显示为 99%，后续核心阶段明确标为尚未开始；
- 保持 v1.2.8 的核心“编译 → 渲染 → 逐页视觉检查 → 定点修复”流水线、现有导出行为和 API 设置
  界面不变，并补充旧页数缺陷、不可变快照权威、篡改拒绝、失败定位和前端阶段展示回归测试。

## v1.2.8（2026-08-23）

- 重构 AI 分析与审阅的核心质量流水线：先由宿主逐行建立不可变 formal 清单；启用第二遍复查时，
  再用独立全文复核器覆盖全部清单项和现有定理环境，关闭时不构造或调用复查客户端并明确记录为
  用户跳过；模型只能针对稳定 ID 给出受限结论，所有修改必须经过哈希、行号、
  边界与可逆补丁安全门，遗漏、错套、多套、重复或清单外条目均 fail-closed；
- 将“编译 → 渲染 → 视觉检查 → 定点修复”搬入生产流水线：OCR 的 AI 模式必须获得真实完整编译
  PDF，先执行确定性逐页检查，再由视觉模型核对每一页；模板重排可合法改变页数，由宿主以内容
  锚点生成带哈希的单调页映射并覆盖两侧全部页面，整书不会在 80 页处截断；最多两轮基于清单 ID
  的定点修复，每轮都重新编译，最终再次盘点 formal 结构并绑定 TEX/PDF 哈希，任一证据缺失即回滚
  并保留诊断草稿；
- 公式编号证据升级为与当前源视觉输入、页内几何和整页渲染哈希绑定的 v2 记录；原始 PDF 直接作为
  视觉来源，原始图片则明确记录“图片 → 单页视觉 PDF”的派生标识、原始/派生字节数和两组 SHA-256，
  包装 PDF 不会被称作 LaTeX 编译结果；
- 工作台新增核心阶段进度与失败定位，明确显示全文清点、全文复核、真实编译、逐页视觉复核、最终
  清单和门禁状态。17 页 Sharp Bounds 固定原始 PDF + 原始 OCR TEX 样本达到 formal/proof 19/19、
  目录 9/9、公式号 6 个、参考文献 27 条与 17 页独立编译；这些数字仅适用于该固定已审阅样本，
  不构成任意 OCR 文字、数学语义或出版质量的通用 95% 保证；
- CI 现在强制运行前端测试后才构建，并以 v1.2.7 作为真实升级冒烟基线。现有“导出 TEX”、
  “导出工程 ZIP”和“一键生成 AI 审计提交包”的外部行为保持兼容。

## v1.2.7（2026-08-23）

- 修复 AI 审计包对 `V:\\lvert`、`C:\\mathcal` 等合法数学命令的路径误判；TeX、BibTeX、
  类/样式文件和 TeX 差异改用证据驱动的专用脱敏器，只允许清理明确凭据、宿主已知路径及路径型
  LaTeX 参数中的绝对路径，URL 与相对资源路径保持原样；
- 新增打包后的内容守恒门与独立 `packaging_status` / `audit_package_status`，逐工件复算字节哈希、
  正文顺序、数学 token、label/ref/cite、环境、花括号和命令；任何未授权变化均闭锁为最小失败包，
  aliases、ZIP 成员、`SHA256SUMS` 与外层 ZIP 哈希可独立复算；
- 正确保留失败编译留下的真实 `PARTIAL_COMPILED` PDF，并记录引擎、遍数、退出码、页数、输入与
  PDF 哈希、致命行和日志；Windows 编译同时抑制 `xdvipdfmx.exe` 崩溃弹窗并安全尝试备用引擎，
  已存在的部分 PDF 不再被源码预览替代；
- 修复 AI review 阶段语义：manifest 和提示词以实际 stage execution map 为准；跳过审阅不会伪造
  reviewed 文件或声称已审阅，相同输出则作为带 canonical path 的逻辑 alias 去重；
- 修复 outline 与页数证据，导出真实 accepted/rejected 大纲、源 PDF 总页数和实际选择页集合；
  空证据不再冒充有效材料，并新增结构化 blocker、`report.json`、`issues.csv` 与 `metrics.json`；
- standard/full 包加入 ElegantBook 等非系统模板依赖、许可证、模板和编译输入清单，并补全按阶段
  区分且不伪造旧运行身份的 runtime/model/prompt provenance；规范包含项目名、工作流、UTC 时间、
  run ID 与源状态的审计 ZIP 文件名；
- 重做“模型与 API 设置”界面，分别配置结构判断、AI 复查和图片/PDF OCR 的模型、Key、Base URL
  与连接状态，同时保留 Codex CLI、本机凭据加密和高级覆盖；现有“导出 TEX”和“导出工程 ZIP”
  行为保持不变。

## v1.2.6（2026-08-22）

- 新增“一键生成 AI 审计提交包”：从宿主冻结的不可变运行快照整理 README、短/完整提示词、
  manifest、输入、阶段产物、预览、报告、验证、决策、日志与证据；支持五种工作流和
  SUCCESS、UNVERIFIED、FAILED、PARTIAL、CANCELLED 全部终态，失败与取消任务也保留已生成阶段；
- 文件角色、状态、SHA-256、父子关系与 VERIFIED 结论只读取宿主和现有机器验证结果；按字节摘要
  去重并记录 aliases，同名文件不覆盖，ZIP 原子落盘，提示词不会引用缺失材料或自行提升验证状态；
- 默认清理 API Key、Authorization、Codex/OpenAI/GitHub 凭据与本机绝对路径；严格区分
  COMPILED、PARTIAL_COMPILED 和 SOURCE_PREVIEW，源码预览明确声明不是 LaTeX 编译结果；
- 审阅工作台新增生成、复制话术、下载、打开文件夹和重新生成入口；TeX、PDF、验证结果或审阅状态
  改变后旧包立即标记 stale。现有“导出 TEX”和“导出工程 ZIP”行为保持不变。

## v1.2.5（2026-08-21）

- 来源记录升级为 v2，并显式区分真正生成结果的 producer 与本次下载工件的 exporter；旧工程缺少
  生成版本证据时会如实记录为 `unknown`，不再用当前应用版本补写。记录同时绑定原始 TEX 的
  字节哈希、LF 规范化文本哈希、包内角色与路径，CI 构建则把确切提交和构建编号嵌入程序；
- 工程 ZIP 新增可独立复算的终态运行包：运行清单、结构化报告、问题 CSV、验证记录、决策记录与
  `SHA256SUMS` 共同记录 SUCCESS/UNVERIFIED/FAILED 等终态、工件血缘和每个包内成员的摘要，
  便于在应用外复核“输入 → 原稿 → 未盖章结果 → 导出主文件”；
- 编译结果改用 `COMPILED`、`PARTIAL_COMPILED`、`SOURCE_PREVIEW` 三态契约；只有实际获得完整
  LaTeX 编译 PDF 才标记为成品预览，失败但留下可读取 PDF 时明确标记为部分编译，没有编译工件时
  只显示源码预览。编译 PDF 以内容哈希隔离保存，并与确切送编主 TEX 及全部子 TEX、图片、样式
  资源的逐文件哈希清单双向绑定，重试失败不会覆盖上次成功工件；原工程遗留的 `main.pdf`、日志和
  辅助文件不能冒充本次输出，旧工程只有源码时安全降级为源码预览。编译日志、返回码、致命错误行
  与多遍编译进度随验证结果保留，Unicode 包名的 `SHA256SUMS` 统一使用 UTF-8；
- 新增证据门控的严格语义 IR：在目录恢复后、模板转换前清点公式、参考文献与前置信息，只在
  PDF 几何证据与活动 LaTeX 清单逐项一致时把印刷公式号归一化为 AMS `\tag`，同时保留原稿的
  左/右编号位置；只在 References 边界及 `[1]` 至 `[N]` 连续编号可证明时生成
  `thebibliography`，并逐条校验全部多行正文、编号唯一性和连续性。独立评分器不再忽略公式号或
  文献号。Sharp Bounds 固定样本回归覆盖 6 个公式号与 27 条参考文献的顺序和正文哈希守恒；
  证据缺失、编号错误、重复或边界歧义时保留原文并闭锁；
- 导出包统一检查大小写不敏感、Windows 路径别名、设备名和文件/目录层级冲突；用户文件不能覆盖
  来源、报告、运行清单或编译预览等保留工件，冲突会明确阻止导出而不是静默替换。

## v1.2.4（2026-08-21）

- 修复 PDF 大纲长标题被按单行截断的问题：OCR 标题候选现在可在同一源页内覆盖连续 1--3 行，
  只有与完整书签标题规范化后完全相等时才会转换；已确认的续行才会删除，未确认后缀、跨页拼接、
  同一大纲标题的额外重复会 fail-closed。对同页另有真实标题的通用 `Heading`/`Bookmark` 占位书签会被
  显式记录并拒绝，不再阻塞真实目录；
- 扩充 OCR formal 全文清点与确定性边界锁：支持 `{\bfseries ...}` 等旧式标题，以及由
  `\textcolor`、`\href`、`\hyperref` 等展示包装的 `Proof of Theorem ...`；带样式的无编号
  Remark 等正式条目也必须进入库存，任何漏套、错套、多套或残留都会阻止正式导出；
- 以 17 页 Sharp Bounds 人工真值样本复核本次问题：9/9 个有效章节标题、13/13 个定理类条目、
  6/6 个证明均精确结构化，正文与数学 token 守恒且只生成一个目录。formal/proof 合计 19/19 的
  人工复核结果仅适用于
  该固定已审阅样本，不代表任意 PDF、OCR 文字或数学语义的通用准确率；
- PDF 公式编号几何清点同时支持正文左、右边缘的独立编号，并要求向内相邻数学内容作为证据，
  排除页眉、列表号和正文行内引用。独立评分工具同步支持人工清单中的旧式/嵌套标题、无编号
  formal、逐项 proof 与多行章节真值，并区分文件二进制哈希和规范化文本哈希。

## v1.2.3（2026-08-21）

- 以用户确认的原始 TEX、旧结构化结果和源 PDF 重新建立人工复核真值：固定样本共 44 个正式条目
  与 11 个证明，独立门禁达到 exact 55/55，precision、recall 与 F1 均为 100%，正文 30,732 个
  token 顺序守恒，24 个大纲节点全部保留并只生成一个目录。该数字只适用于这份已审阅样本，
  不代表任意 OCR、数学语义或出版质量的通用保证；
- OCR 文档新增生产级正文零丢失门禁，并与实际定理/证明扫描规则共享同一标题元数据；英文、中文、
  无编号、样式包裹、Proof 附加说明和命名定理均有真实流水线回归。命名标题不会再被当作可丢弃
  结构词；正文缺字、错套、多套、边界跨段或残留显式标题都会 fail-closed；
- faithfulbook 改为优先保留源标题与作者，只在明确选择项目标题策略时替换；语义分页只清理带 OCR
  所有权证据的硬分页，普通作者分页保持不动。PDF 公式编号与脚注新增文档级清点：公式编号必须在
  AMS 展示环境中以活动 `\tag{15}` 形式按源页顺序逐项匹配，`\tag{(15)}`、顺序交换、清点器
  失败或脚注缺失都会阻止出版审校导入；
- TEX 与工程 ZIP 新增 VERIFIED/UNVERIFIED 来源记录，绑定原文、结果、版本、提交和提示版本哈希，
  同时明确验证范围；保留 UTF-8/UTF-16 BOM、原换行及文件首行 TeX magic，避免来源标记改变编译器选择。

## v1.2.2（2026-08-20）

- OCR 出版审校为证据充分的显式编号 formal 结构及其证明加入确定性锁：定理、引理、问题、
  猜想、定义、问句与证明只有在边界与原文哈希同时匹配时才应用，避免漏套、错套或跨段吞入；仍有
  显式 formal 标题未被结构化时会 fail-closed，不再把不完整结果标为可正式导出；
- 可靠章节大纲现在自动生成且只保留一个目录；模板转换同步保护页眉附近正文和展示数学后的
  尾句，并只清理展示数学内部的无效空段，使 References 等结构层级与书籍模板保持一致；
- 新增独立结构准确率门禁，核对环境类型、编号、起止边界、正文与目录。55 项人工审阅回归
  语料达到 exact 55/55（precision、recall 均为 100%），正文 token 完全守恒；这是固定语料
  与明确评分口径下的范围内回归结果，不代表所有文档、OCR 文字或数学语义的通用准确率保证。

## v1.2.1（2026-08-20）

- 将整书实验中验证有效的做法收敛为通用“出版审校”OCR 工作流：GUI 默认至少 200 DPI，
  Codex 的低推理强度会按任务提升到中等；低置信、待人工复核或视觉来源记录缺失会阻止进入
  结构整理，但原始 TEX、图片与诊断工程仍可保存；标准转写继续作为更快的兼容模式；
- OCR 工程新增独立质量报告和来源链：记录原文件、视觉输入、文字层摘要、局部像素核验、
  图片裁片及模型配置的哈希/元数据；导入项目时保留不可变原始 PDF/图片，便于后续逐页与数学
  审计。流程门通过只表示未发现已知阻断，界面与 manifest 明确标注准确率尚未测量、出版就绪
  尚未建立；
- 成品版式改为 OCR 启动前冻结的模板选择，默认仍为适合图书的 `faithfulbook`，也可主动保持
  原排版或选择 ElegantBook。模板在结构分析前应用，AI 只填结构与内容，不自由生成整套样式；
  模板元数据现公开版式是否改变及对应 QA 档位；
- 本地 Codex 的出版审校新增通用复杂公式高清证据：只在带可靠数学字体层的 PDF 上定位每页
  最多 4 个高风险展示公式，直接从源 PDF 以 420 DPI 裁切，并与整页图合并为同一次多图请求。
  公式裁片采用严格结构、哈希、尺寸、边界与内容寻址元数据校验；API、标准模式、扫描页和低
  置信候选保持原行为，系统不自动替换不确定公式，也不把证据门通过等同于数学准确率；
- OCR 导入与导出证据链改为 fail-closed：后台分析任务真正启动后才提交幂等导入记录；保存、
  导入和工程打包都会重新校验原文件及逐页视觉输入，出版审校工程包同时携带哈希绑定的
  `ocr-source.*`、`OCR-QUALITY.json` 和资源清单，缺失、篡改或扩展名伪装会阻止正式导出。

## v1.2.0（2026-08-20）

- 统一了 Codex 视觉 OCR 与文本分析/独立审阅链路：视觉阶段逐页转写受控图像，
  文本阶段只提交可逆结构决策；暂停、续跑、失败页重试、中间快照和带警告导出仍然保留。
  本机 Codex CLI 只复用 ChatGPT 登录和订阅额度，推理仍需联网；未就绪时明确停止，
  不会静默回退到按量 API；
- OCR 整理默认使用 `faithfulbook` 155×235mm 双面书籍近似版式，将样式层内联到主 TEX，
  并在证据充足时重建章节、目录、奇偶页眉与图片资源。缺少大纲、图片、数学或编译证据时
  保守保留或标记 `UNVERIFIED`，不把未通过门禁的结果冒充成品；
- 提供两种面向开发者的整书工作流：可恢复的 Codex OCR/分析跑批，以及一个 Bondy
  17 章验证用的快速混合工具。后者保留原 PDF 矢量页、可搜索文字和嵌入字体，只补书签与哈希
  manifest；它不是 OCR 重排版，也不是通用 PDF 转换器；
- 软件仓库、安装包、便携包和 GitHub Release 都不包含测试书籍、书页图像或生成的整书 PDF。
  用户必须自行提供有权使用的源文档，所有中间物与派生输出仅保存在本地指定目录。
  OCR/AI 结果仍需人工审阅；样本门禁或单本书实验不代表任意文档的通用准确率，也不代表可直接用于出版。

## v1.1.11（2026-08-19）

- 新增统一的本机 Codex CLI 后端：分析、独立审阅以及 PDF/图片逐页 OCR 都可复用本机
  ChatGPT 登录和套餐内 Codex 额度，不再要求为视觉识别单独配置按量 API Key；API 模式仍作为
  用户主动选择的兼容选项保留；
- 安装包固定并内置官方 `openai-codex-cli-bin` 运行时，设置页显示运行时、登录与就绪状态，
  并支持选择模型和推理强度。OCR 使用 Codex CLI 官方图片输入，在临时隔离目录中逐页处理，
  保留既有暂停、继续、失败页重试、图片资源打包和进入结构化审阅的完整链路；
- Codex 后端仅接受 ChatGPT 登录，使用最小子进程环境且不继承项目 API Key；未登录、检测到
  API Key 登录或调用失败时明确停止并保留原文，不会静默回退到按量计费 API。该模式仍需联网，
  运行会消耗 ChatGPT/Codex 订阅额度，并非离线模型；
- 安装版随程序安装 OpenAI Codex CLI 的 Apache-2.0 许可证与上游 NOTICE；便携版改为包含
  `LaTeXStruct.exe` 和同一份可读第三方声明的 ZIP，确保二进制再分发保留完整许可与归属。

## v1.1.10（2026-08-19）

- 继续加固 OCR 逐页恢复链路：视觉模型拒答、截断、结构化输出或 PDF 渲染失败均不再
  把页面留在不可操作状态；新增安全暂停/继续、按原 DPI 重新渲染和全部失败页批量重试，
  并修复页签切换时的任务状态竞态；
- 原始 OCR 默认保存为包含 `ocr.tex`、图片、源页回退图和哈希 manifest 的完整工程 ZIP；
  导入、分析、审阅与 XeLaTeX 临时编译使用同一组已校验资源，修复图片双扩展名和编译
  目录遗漏附件造成的“缺少图片”；
- 分析、编译或审阅尚未通过时，仍可随时取回当前 TEX、完整工程 ZIP 和报告；这些快照会
  显式标记 `UNVERIFIED` 与警告，不会被冒充为已验证成品，上一份安全结果也不会被覆盖；
- 普通 TeX 默认改为保持原文档类、宏包、章节层级和自定义环境，ElegantBook 改为显式可选；
  单文件和文件夹项目在处理后按原编码、BOM 与换行风格写回，覆盖 UTF-8/UTF-16、GBK
  和 Latin-1，无法保真编码时闭锁而不损坏原文件；
- 提高 AI 分析与审阅的可靠性：决策必须覆盖完整候选，定理/证明只能在可靠结束点闭合，
  短截断、跨结构边界和多原子合并一律闭锁；扫描器排除超链接、表格/对齐环境和伪证明标题，
  并识别可达的外部 `.cls/.sty/.tex` 自定义定理环境；普通项目也会比较修改前后的实际编译结果，
  同一项目的分析、审阅和提交在单一事务锁中完成；
- 引入正式外部准确率门禁：三本数学书和三篇数学论文中的 720 个封闭盲测样本全部通过，
  自动修改、保留原文和人工升级各类别的点估计及 95% Wilson 下限均达到发布阈值，
  且六份文档均逐项完全匹配。语料来源、隔离规则、哈希和可复现评分见
  [外部准确率报告](benchmark/EXTERNAL_ACCURACY_REPORT.md)。

## v1.1.9（2026-08-17）

- 修复 v1.1.8 把视觉模型提前生成的章节、定理或证明命令整页丢弃、并用相同提示稳定重试失败的
  OCR 回归；现在会保留已识别正文并标记为低置信供人工复核。PDF 中途渲染失败的待处理页也会
  明确转为可恢复状态，可按原 DPI 重新渲染并一键重试全部失败页，不再出现无法点击的永久
  `pending` 页面；截断的视觉响应不会再冒充完整成功结果；
- OCR 新增真正的安全暂停与继续：当前付费页面调用完成后停在下一页之前，已完成页面、实时 TEX、
  Token 与费用均保持不变，刷新页面后仍可恢复任务。暂停、继续和批量重试均有服务端幂等门禁，
  不会重复识别已成功页面；
- 修复显式图片扩展名被保存成 `.png.png`、资源检查又误把双扩展名当作存在，以及 XeLaTeX
  临时编译目录没有带入真实图片的三个资源链路问题。可提取插图继续保存为真实局部图；无法可靠
  分离的扫描页使用带清晰 manifest 标记的源页回退图，避免把缺失引用带进分析与审阅；
- 原始 OCR 主保存改为包含 `ocr.tex`、图片和 manifest 的工程 ZIP，浏览器备用下载同样可取得
  完整包；结构化工程 ZIP 也保留哈希校验过的 OCR 图片与源页预览；
- 分析或编译检查未通过时，仍可导出带 `UNVERIFIED` 警告的当前 TEX、工程 ZIP 和汇报；原有
  “已验证 ElegantBook 成品”接口、哈希校验与上一次安全版本完全保留，界面会明确区分两种输出，
  不再把可编译性误当成用户取回自己草稿的权限。

## v1.1.8（2026-08-17）

- 修复 AI 复查把“修改后预览”的行号误当成原文行号、从而在最后阶段把
  `\end{theorem}` / `\end{proof}` 移入展示公式的问题；复查坐标现在明确区分源锚点与结果坐标，
  错误范围会撤销对应建议而不是重写原文，数学环境内的边界统一吸附到最外层完整公式之后；
  长证明若在 240 行安全窗口内看不到可靠停止点会直接拒绝，绝不生成半个环境；
  即使模型在完整窗口内只选择证明开头，程序也会确定性扩到首个 QED 或下一可靠结构边界，
  仅有含歧义的文档尾时则整项不改；复查结论只能作用于当前批次实际展示的候选，不能跨批引用；
- OCR 的视觉阶段只负责忠实转录，模型若提前生成章节、目录或定理环境会触发单页重试；结构阶段
  以 PDF 大纲和每段唯一物理页标记重建 `\chapter` / `\section` 层级，删除可证明为手抄目录的
  `\dotfill` 区域并只插入一个真正的 `\tableofcontents`，同时清理重复页眉、印刷页码和标题前导
  `\quad`；AI 或 AI 复查不可用时明确停止并保留原文，不再静默冒充 AI 结果；
  注释或代码区中的假 `\tableofcontents` 不再满足目录门禁，视觉模型输出完整文档外壳会触发
  单页重试，页尾 `\hfill 8` 等印刷页码会被安全清理或阻断导出；
- 修复 OCR 图片文件名中的印刷页码与 PDF 物理页不一致导致取错页、缺图或裁掉标签的问题；导入时
  从原 PDF 提取并留边保存真实局部图，同页并排图分别裁切、组合图整体裁切，拒绝用整页扫描图
  冒充插图，完整工程 ZIP 只包含哈希校验通过的真实资源；
- 安全检查失败时保存独立、带哈希的诊断草稿和可行动原因，应用重启或重新打开项目后仍可查看；
  诊断草稿不能接受、拒绝、应用或导出，也不会覆盖原项目及上一份已验证结果。编译错误会保留
  LaTeX 命令和行号，同时去除本机绝对路径；
  最终四个提交文件中任一个写入失败时，会逐字节恢复上一份结果、汇报、审阅清单和验证标记，
  不再只恢复标记而丢失上一份可用成果；
- 新建、拖拽、ZIP 与 OCR 导入默认使用 AI 深度整理；规则模式只作为旧项目兼容入口保留。真实
  24 页失败样本现覆盖目录、章节、定理/证明、展示公式、页眉页脚及 5 个插图的离线回归；
- 将用户确认的品牌图标接入 Windows EXE、安装器、卸载项、桌面窗口、网页图标、顶部栏和更新弹窗，
  并在 CI 明确安装图标生成依赖；
- 上一版升级冒烟现在真实经过应用内 `schedule_installer_after_exit` 独立 helper，再验证旧进程退出、
  新版连续健康、React 首页及带哈希静态资源可读和更新结果版本一致，防止“更新后关闭但不再启动”
  以及只启动后端、界面资源缺失的假成功。

## v1.1.7（2026-08-16）

- 修复 AI 复查已完成、实时草稿与安全检查均正常，但在最后保存审阅清单时把内部
  `AppliedPatch` 对象写入 JSON，导致进度到 100% 后显示“处理未完成”的问题；项目现在只持久化
  复查发现、无效响应、Token 用量和错误摘要，运行时补丁、决策对象及中间文本不会进入项目文件；
- 最终结果提交增加 JSON 预检：在移动旧验证标记或写入 `result.tex`、汇报、决策之前，先验证
  整组数据可以稳定序列化；若未来再有内部对象越界，上一份已验证结果会逐字节保持不变；
- 增加包含 48 个真实 `AppliedPatch` 的后台完成回归，以及序列化失败时四个提交文件零改动的
  存储回归，覆盖普通项目、OCR 导入后的后台整理和文件夹项目共用的提交边界。
- 修复自动更新关闭旧版后偶发不再启动的问题：v1.1.7 安装器自带独立恢复器，等待旧服务释放
  端口，必要时从真实安装目录重试启动，并以新版 `/api/health` 响应作为成功条件；连续失败会
  保留无密钥诊断日志并提示用户从桌面或开始菜单重新打开。该恢复器由新安装器执行，因此
  v1.1.6 发起升级时也能生效。恢复脚本保持纯 ASCII，中文提示在运行时解码，避免 Windows
  PowerShell 5.1 按系统代码页误读无 BOM 的 UTF-8 脚本并在启动前语法失败。

## v1.1.6（2026-08-16）

- 正式导出固定为 ElegantBook v4.7，不再让新手在“保持原排版/模板”之间误选；文章型输入的
  `section/subsection` 按已解析语义整体上移，已有 `chapter` 不重复转换，目录只依据原命令或
  OCR 的 PDF 大纲重建，不再凭数字标题猜测、删除或推进章计数器；没有用户封面图时改用
  自包含的简洁标题页，不再显示 ElegantBook 的 `example-image` 占位图；目录使用前置页码，
  正文从第 1 页重新编号；
- 定理类统一使用 ElegantBook 的无编号星号色块，把 OCR/原书显式编号作为标题参数保留，避免
  自动编号与原编号重叠；补齐 ElegantBook 未内置的常用猜想、注记、例题、问题和练习环境；
- 导出提供“ElegantBook TEX”和“完整工程 ZIP”两种选择；ZIP 包含主 TEX、原项目图片/子文件、
  固定且校验 hash 的 `elegantbook.cls`、LPPL 许可证、工程说明和同次提交的安全汇报；旧的
  非 ElegantBook 结果即使有旧安全标记也必须重新分析，不能在导出时绕过流水线临时换模板；
- OCR 完成后可明确选择“AI 深度整理”或“规则整理”，不再固定为规则模式；导入只创建保留
  原始 OCR 的项目，随即启动标准可暂停后台任务并进入工作台，结构化 TeX 草稿按安全批次实时增长；
  两种整理方式最终都进入同一 ElegantBook 模板与安全检查；

## v1.1.5（2026-08-16）

- PDF OCR 会读取书签树并只保留本次所选页范围内的节点，按 `article/book` 语义统一重建
  `chapter/section/subsection/subsubsection`；相邻的 “Chapter N” 与章名会合并，静态
  `\dotfill` 目录只在整段可证明为目录框架时替换为 `\tableofcontents`，混入未知正文时
  保留原文并阻止导出；
- OCR 的加粗/斜体定理、引理、命题、定义、注记、猜想、问题、习题及证明标题可进入真实
  theorem-like/proof 环境；显式源编号只保留一次，正文范围可跨展示公式、列表与 OCR 页边界，
  `Proof of Theorem 2.7` 等说明不会被拆成第二个标题；
- OCR 项目新增 PDF 大纲层级、真实图片资源和编译结果安全门；缺图、越出项目的
  `\graphicspath`、展示公式错误或可用 XeLaTeX 下编译失败时一律 `safe_to_export=false`，
  汇报分别列出章节、资源和编译原因，不再用“结构化前后路径一致”掩盖原始 OCR 错误；
- 目录/交叉引用类文档的编译检查执行两遍 XeLaTeX，使目录页码与引用目标参与验收。
- 自动更新改为完整弹窗流程：显示当前/目标版本、中文更新说明、真实下载百分比和字节数，
  支持在校验前安全取消；失败不会关闭当前应用，安装包完成大小与 SHA-256 校验后才会退出、
  静默安装并重启；安装器会把旧版本号交给新版本，首次启动显示“更新成功”及本次更新内容。

## v1.1.4（2026-08-15）

- 修复 v1.1.3 的 Windows 发布包遗漏已构建 React 前端资源、导致安装版和便携版无法正常
  进入主界面的问题；v1.1.4 将前端资源重新纳入发布包。

## v1.1.3（2026-08-15）

- 修复 Windows 桌面 WebView 中浏览器下载可能被宿主拦截的问题：`result.tex`、多文件项目
  ZIP、整理汇报和原始 OCR 均可原子保存到固定的“下载/LaTeXStruct”目录；同名文件永不
  覆盖，并提供一键复制、打开保存位置和浏览器备用下载；
- 正式结果继续复用 `safe_to_export`、提交标记和 SHA-256 门禁；汇报增加独立 hash，项目
  ZIP 也只包含同一次安全提交的汇报，同时兼容 v1.1.2 及更早版本尚无汇报 hash 的项目；
- OCR 在既有 1-based 起止页范围上增加逐页累积的实时 LaTeX 草稿，可在整本进度和单页
  原图/转写之间切换；结果按 revision 保全，保存、重试和导入互斥，重试后不会误把旧文件
  标记为最新成果；
- OCR 当前任务会在页签切换后恢复进度、页面和实时预览；重试请求断线时继续确认后台状态，
  尚未下载或导入的付费终态结果不会被 TTL 清理，也会继续阻止可能丢失成果的应用更新；
- 结构化处理按 AI 批次更新明确标注的未验证草稿，并显示候选进度、当前动作和草稿版本；
  最终结果仍须经过正文、数学、引用、图片、环境及编译等安全检查后才可导出；
- 审阅侧栏增加结果概览、逐项安全检查和安全渲染的 Markdown 汇报；重新分析期间禁用旧结果
  的保存/复制操作，避免把上一轮结果误认为当前实时成果；
- OCR 与结构化工作台改为近全窗口代码主视图：实时模式只保留窄页码/决策栏，把主要空间
  交给累积 LaTeX 与 Diff，并提供不卸载编辑器的“专注预览”；已有 OCR 任务未明确保全或
  放弃前禁止重复启动，避免新的付费任务覆盖当前结果；
- 图片与 PDF 统一为“先检查文件、再启动”的高熵任务编号流程；启动响应中断后可安全恢复
  或重放同一任务而不会再次调用模型，恢复状态不明时默认锁定新建操作并提供重试；正文、
  逐页状态和费用分别记录版本，任一最新状态未保全都会阻止更新或自动清理。

## v1.1.2（2026-08-15）

- 重做 Windows 自动更新交接：更新包按 GitHub Release 的大小与 SHA-256 原子校验，当前
  程序退出并释放文件后再静默安装；新安装器能兼容 v1.1.1 的旧更新参数，避免停在
  “无法自动关闭应用程序”的重试/忽略对话框；
- 更新前会阻止新的结构化或 OCR 任务，并在仍有运行、暂停或尚未保存的付费 OCR 成果时
  拒绝关闭；更新辅助进程使用最小环境，不继承 API Key 等敏感变量；CI 增加真实
  v1.1.1 运行中升级到当前版本的回归测试；
- PDF OCR 改为一次上传后先读取总页数，再选择 1-based 起始页和结束页；单次最多 500 页，
  超长 PDF 默认选择前 500 页，实际渲染、Token 与费用只统计所选范围；
- OCR 进度同时显示原 PDF 页码和本次任务序号，例如“原第 88 页 · 1/3”；页树、完成态、
  部分失败、重试和费用提示统一使用真实成功页数，并防止同页重复重试造成重复调用；
- 处理进度轮询支持断线退避与手动重连；实时草稿增加内容版本号，只有正文变化时才重新
  下载，大文本自动降低刷新频率；项目列表保留完成、错误和取消状态并显示可行动原因；
- 审阅与 OCR 页树改用键盘可达控件，修复下载控件嵌套；错误提示按认证、文件、编译、
  安全检查和解析问题分别给出下一步，不再一律要求检查 AI 设置。

## v1.1.1（2026-08-15）

- 模型请求统一设置可配置的最大输出 Token、重试次数与重试间隔，让单次费用上限和瞬时
  故障重试可控；密钥字段不再出现在配置对象的调试表示中；
- API 请求默认强制 HTTPS（仅本机 loopback 可用 HTTP）、拒绝重定向，并按规范化的精确
  Host/官方域名后缀识别供应商，避免把自动加载的 Key 带到相似或被替换的地址；
- 强化配置与系统凭据管理器的原子保存：切换 API Host 时要求同时提交新 Key，环境变量
  覆盖不会污染磁盘配置，凭据写入或删除失败时恢复原状态并阻止保存；
- `DASHSCOPE_API_KEY` 可为 Qwen 的结构判断、AI 复查和 OCR 三个角色供给凭据；OCR
  provider 与三个角色的 Base URL/model 环境覆盖会经过能力和地址校验，保存时保留本地值；
- 设置页按服务商给出新手选项，非预设模型与实际 Host/模型保持一致；切换平台或地址会清除
  待保存 Key。OCR 在模型、视觉能力或 Key 未确认时给出可行动提示并阻止误启动；
- 对官方 DeepSeek 与 Qwen 请求显式关闭思考模式，减少额外正文对 JSON 决策解析的干扰；
- AI 采用扫描器确定提取的标题前缀和定理/证明编号，避免结构包装后重复显示原编号或标题，
  同时不采用模型自由生成的 LaTeX 可选参数；
- 自动新增的 theorem-like 声明改用无编号环境；原书已有显式编号但目标环境编号语义不明确时
  保守留待人工确认，并在最终应用阶段约束复查范围，避免越过扫描器确认的标题行；
- 无编号中文“定理：”“注：”“例。”等标题会安全消费紧随标点；标题后正文为空时仍保留原文；
- 结果文件与最终 verification 记录使用 SHA-256 绑定并最后提交；记录缺失、写入中断、旧 hash
  或安全检查未明确通过时，单文件与文件夹导出均默认阻止；
- OCR 会把唯一完整的带 tag 展示公式保守规范化为 equation；残留非法 tag、不完整或次序
  异常的 display 会标为低置信度，并由最终安全检查阻止导出；
- AI 设置加载失败时提供明确重试入口；OCR 完成、部分失败和错误状态分别显示真实成功页数与
  可执行的下一步，不再把失败页面计作“全部完成”；
- 汇报会按原因聚合原书中重复的已知问题，保留代表行号与总数，避免同一问题刷屏；
- 发布脚本校验应用版本、输出路径、旧产物清理和构建退出码；CI 安装冒烟核对运行版本，
  证书与调试文件加入忽略规则，并移除已误入仓库的调试产物。

## v1.1.0（2026-08-15）

- 结构化处理改为可观察的后台任务：显示分阶段进度、当前动作和最近步骤，支持安全暂停、
  继续、取消；正在进行的模型请求会先完成，再在批次边界暂停；只有完成全部安全检查后
  才进入不可中断的原子保存窗口；
- 工作台可在处理中查看明确标注的实时草稿，并持续显示已判断候选、Token 用量和约人民币
  费用；未知自定义模型只统计 Token、不虚构价格；OCR 页同步显示分页进度和费用估算；
- AI 设置重做为“选择平台 → 填一次 API Key → 自由切换模型”，默认收起 Base URL、分角色
  Key 等高级参数；增加 DeepSeek V4 Flash/Pro 与 Qwen 文字/视觉预设，并把已退役的
  `deepseek-chat` / `deepseek-reasoner` 配置无感迁移到现行型号；
- 导入页支持拖拽单个 `.tex`、完整文件夹和 ZIP；ZIP 会去掉唯一外层目录、智能识别主文件，
  并防护路径穿越、大小写冲突、加密条目、超限体积和异常压缩比；导入与分析分离，避免
  用户尚未确认就开始调用 AI；
- 项目列表显示活动任务并可直接暂停/继续；活动任务期间阻止删除和并发修改审阅结论；
  取消任务不会保存未验证草稿，已有安全结果继续保留；
- 增加费用分档、后台任务状态机、ZIP 安全与真实 FastAPI 接口测试；生产前端通过真实
  Chromium 验收（ZIP 拖放、模型切换、处理进度、实时 Diff 与安全结果）。

## v1.0.0（2026-08-14）
- 接入 Alibaba Cloud Model Studio / Qwen OpenAI 兼容视觉模型预设；按 2026-07
  官方型号补充并首选原生视觉模型 `qwen3.7-flash`，同时保留 `qwen3-vl-flash`、
  `qwen3.6-flash`、`qwen3.7-plus`；
- OCR 支持 `DASHSCOPE_API_KEY`、provider/base URL/model 环境变量；环境变量 Key
  不会因保存设置而写入 config.json 或 Windows 凭据管理器；
- 修复 JPG 被误标为 PNG 及预览 MIME；补充安全的 HTTP 错误分类、密钥脱敏、
  Qwen 多模态离线测试与在线冒烟脚本。
- Parser/Scanner 对嵌套环境、注释、verbatim/minted、数学环境、自定义 theorem 与
  错配环境采取保守处理；同名环境实例独立判断，结构边界不确定时不自动移动；
- 修复“正文使用过 theorem”被误判为“导言区已声明 theorem”导致输出不可编译；已加载
  amsthm 时只补缺失声明、不重复加载宏包；编译基准失败现在会返回非零状态并真正阻断 CI；
- 重构安全门：正文、数学、label/ref/cite、图片路径、环境、花括号、多文件依赖、
  文件集合和编译对比统一汇总；失败阻止导出并回滚可逆模板修改；
- 多文件导入增加路径、数量、大小和内部标记校验，保留图片/bib/sty 等二进制资源；
- OCR 增加逐页进度、瞬时失败自动重试、失败页手动重试、部分结果恢复，并将 raw OCR
  与 structured 结果隔离；
- 审阅页补齐前后项、接受/拒绝/撤销、筛选、低置信度与安全检查展示；决策状态变化
  复用缓存，避免重复 AI 调用；超大文本改用压缩 diff/暂停全书双栏渲染；
- 密钥写入与配置文件保存改为事务式、原子写入；凭据管理器不可用时明确失败，不再
  静默回退明文；`.env`、`.env.*` 和本地 secret 文件持续禁止入库；
- OCR 运行依赖加入安装元数据；前端、Windows 版本资源与安装器统一跟随 `_version.py`；
  CI 改用 pytest 自动发现全部测试并在常规分支构建前端。

## v0.8.0（2025-08）
- **系统凭据管理器（keyring）**：开启后 API Key 写入 Windows 凭据管理器
  （advapi32 CredWrite/CredRead/CredDelete，零依赖），config.json 只存 `__keyring__`
  占位符不再落明文；关闭时自动清理系统凭据；非 Windows 平台自动回退原明文行为；
  设置页开关 + 「已配置(系统凭据)」来源标识；后端可注入测试（FakeBackend 6 例）。

## v0.7.1（2025-08）
- Monaco 三栏审阅工作台：决策树（按章节分组）+ 双向 Diff 编辑器（行号居中定位）+
  检视器（单项拒绝/同类批量拒绝/全部重置），三栏联动；
- OCR 逐页验证：任务进度按页细粒度展示、页面 PNG 预览 + LaTeX 逐页校对、
  低置信度标注（[?]/过短/失败）与单页重试端点；无 Key 时任务优雅完成并逐页报错；
- /legacy 目录重定向修复 + OCR 状态接口内部回调键过滤；服务器测试扩至 7 组（113 例）。

## v0.7.0（2025-08）
- React+Vite+Monaco 前端重建（本地打包 Monaco，离线可用；旧 UI 移至 /legacy）；
- 后端能力全暴露：文件夹导入（依赖图展示）、规则包选择、结构化文件夹 zip 导出、
  三角色模型设置；CI 自动构建前端并打进安装器。

## v0.6.2（2025-08）
- Benchmark 扩充：14 金标集 / 200 标签（真实书稿 10 组开发 + 2 组 hold-out），
  汇总 P/R/F1 + 正文零改动率/引用保持率/编译成功率；行号锚定标签；
- 基线对比环境平衡（部分输入原文不平衡时按"不新增不平衡"门禁）。

## v0.6.1（2025-08）
- Compile CI：TinyTeX + Benchmark --compile 前后编译对比门禁（报告编译列真实数据）；
- Lint 门禁升级（F+E741+W605）；pyproject 动态版本核实。

## v0.6.0（2025-08）
- 审阅式 UI：决策清单（章节/环境/行号/置信度/原因/状态）+ 过滤器 +
  单项拒绝重跑（exclude 重处理 + 全量重新校验）+ diff 行号定位；
- 评审重构收官：P0→P2 八轮全部落地（见 设计文档.md 评审重构总报告）。

## v0.5.6（2025-08）
- OCR 两阶段解耦：Stage A 视觉忠实转写（禁止结构判断），Stage B 复用结构化流水线；
  ocr_pipeline() 一步到位，两阶段语义测试锁定。

## v0.5.5（2025-08）
- Benchmark 金标评测：四组金标集（合成/中文/真实切片/陷阱），
  分类型 P/R/F1 + 内容指标 + Markdown 报告；CI 门禁；当前成绩全 100%、零误报。

## v0.5.4（2025-08）
- Rule Pack 配置化：内置 bilingual/english/chinese/academic-paper 规则包 +
  用户自定义 JSON（标题关键词映射/证明起始与续段词/习题关键词/双语开关），
  全链路 pack 参数贯通；default 包与旧行为完全一致。

## v0.5.3（2025-08）
- 多文件 LaTeX 项目支持：main.tex 发现、\input/\include 依赖图（缺失/循环检测）、
  带标记展开→流水线→拆分回文件→副本导出；CLI tools/process_project.py。

## v0.5.2（2025-08）
- 平衡括号解析器（core/texparse.py）：节标题支持嵌套花括号与数学内花括号、
  可选参数嵌套；双语翻译框识别同步升级；真实书稿回归零差异。

## v0.5.1（2025-08）
- 多层内容不变量校验：数学公式 token 多重集、`\label`/`\ref`/`\cite`/图片路径集合，
  整理前后必须完全一致（纳入 ok 判定）；
- 编译校验（可选，本机有 xelatex 时）：整理前后各编译一次对比页数与错误；
- 汇报展示五类不变量与编译对比；test_invariants 套件（含真实 xelatex 编译测试）。

## v0.5.0（2025-08）

### 修复（P0）
- 版本号单一事实来源：`latexstruct/_version.py`，pyproject 动态读取，CI 校验 tag↔版本；
- CI 新增安装器冒烟测试（静默安装 → 运行健康检查 → 静默卸载），发布链路全环节验证；
- 清理误提交的 CI 调试产物（jobs.json）并加入 .gitignore。

### 文档
- 补齐 LICENSE（MIT）、CHANGELOG、CONTRIBUTING；设计文档入仓。

## v0.4.0（2025-08）
- 复查分块（review_batch，整书规模）；全书级 AI 实测（Godsil 3.58 万行：827 补丁、735 项复查修正、内容不变校验通过）。

## v0.3.3（2025-08）
- 决策提示词强化（PROMPT_VERSION 3.1：盒内翻译/(a)(b)条目/图注否定示例、span 语义）；
- 漏报抽查复查（AI 判 none 的候选交复查可 missed-extra 反悔）；
- CI ruff 质量门禁。

## v0.3.2（2025-08）
- AI 决策 span 段落边界合法化（wrong-range 10→1、复查 token -36%）；
- 复查成本优化（规则确定性编辑免复查）。

## v0.3.1（2025-08）
- OCR 友好错误提示（非视觉模型 400）、OCR Key 回退链修复；
- 构建脚本 --clean（修复 PyInstaller 增量缓存打包旧版本）；
- 安装器本地 E2E 验证；真实书稿 1.7 节回归样例。

## v0.3.0（2025-08）
- OCR 转写模块（PDF/图片 → 视觉模型逐页转写 → ElegantBook 书稿，模型可选）；
- 真实 AI Key 全流程验证（决策 + deepseek-reasoner 复查真实纠错）；
- CI 代码签名（secrets 配置后自动签名 exe 与安装器）、版本资源、自助签名脚本；
- 中文安装器默认界面。

## v0.2.1 / v0.2.0（2025-08）
- 安装版客户端（PyInstaller 单文件 exe + Inno Setup 安装器）与主动更新
  （GitHub Releases 检测 → 下载安装器 → 静默升级，E2E 验证）；
- GitHub Actions 全自动测试/构建/发布流水线。

## v0.1.x（开发期）
- 核心引擎：解析（等长屏蔽/精确偏移）→ 规则扫描 → AI 决策 → 可逆补丁 →
  内容不变机器校验 → AI 复查 → 汇报；
- 真实书稿打磨（双语盒、编号提取、整段证明、习题节、ElegantBook 模板转换、目录替换）。
